# Vibe Session — Simplify README and config

**Started:** 2026-03-02 09:00

## Goal

Decompose `README.md` into sub-documents under `/docs`, and investigate `examples/hal.config.json` to understand current hard-coded defaults and define a minimal project setup.

---

## Context

**Project:** HAL — a Telegram bot launcher that wraps AI coding CLIs (Claude Code, Copilot, Codex, OpenCode, Cursor, Antigravity/Gemini) and exposes them per-project via individual Telegram bots.

**Tech stack:** TypeScript, Node.js 18+, Grammy, Pino, Zod, Biome, pnpm.

**Recent work (last completed tasks):**
- 026 — Google Antigravity engine
- 023 — Sort commands in /help and Telegram UI
- 021 — Switch model command
- 011 — Cursor engine adapter (most recent)
- 015 — Config hot-reload

**Relevant backlog:**
- 022 (Ready) — Default engine auto-detection; ties into minimal config design (`init` command output)
- 027 (Draft) — YAML config support
- 028 (Draft) — JSON++ config with comments
- 029 (Draft) — Default models / providers config and validation

**Areas of interest:**
- `README.md` (1187 lines) — primary target for decomposition
- `examples/hal.config.json` — reference config with several non-minimal defaults
- `src/config.ts` — where defaults are enforced in code

---

## Log

### 2026-03-02T0900 — Session start

Loaded project context. README is 1187 lines with ~12 logical sections. Identified 9 candidate `/docs` sub-documents. Audited `examples/hal.config.json` and found 7 issues with non-minimal or confusing defaults.

README decomposition target files:

| File | Content |
|---|---|
| `docs/configuration.md` | `hal.config.json` basics, local config, env vars, `globals`, `projects[]`, `dataDir`, log files |
| `docs/context.md` | Context injection — implicit context, custom context, variable patterns, hooks, prompt format |
| `docs/commands-config.md` | `commands` config block — enabling/disabling, custom messages, `${HAL_COMMANDS}`, skills visibility |
| `docs/engines.md` | All 6 engine configs (Claude, Copilot, Codex, OpenCode, Cursor, Antigravity), providers, model defaults |
| `docs/custom-commands.md` | `.mjs` command files, handler args (`args`, `ctx`, `gram`, `agent`, `projectCtx`), file sending |
| `docs/skills.md` | Skills standard, skill directories per engine, hot-reload |
| `docs/voice.md` | Voice message transcription, Whisper models, setup (ffmpeg, cmake) |
| `docs/telegram-setup.md` | Creating a bot via BotFather, finding user ID |
| `docs/migration.md` | Migration from v1 single-project config format |

`examples/hal.config.json` audit:

| Issue | Detail |
|---|---|
| `_engine` prefix | Disabled via `_` prefix — undocumented pattern, confusing |
| `providers` block | Model names that may go stale; not needed in a minimal example |
| `logging.persist: true` | README default is `false`; override without explanation |
| `transcription.model: "medium"` | README default is `base.en`; medium is 1.5 GB — bad first-run default |
| `context.custom` | Example noise, not meaningful for a minimal config |
| `__dangerouslyEnableYolo` prefix | Double-underscore to disable — inconsistent with `_engine` |
| `active: true` | Redundant since it is the default |

Minimal config identified as requiring only: `projects[].name`, `projects[].cwd`, `projects[].telegram.botToken`. All other settings have sensible built-in defaults.

### 2026-03-02T1015 — Created `vibe-code` skill

Created `.agents/skills/vibe-code/SKILL.md` to standardize future vibe coding sessions. The skill creates a dated log in `vibed/`, loads project context (BACKLOG, README, docs), scans the codebase, writes a context digest, and maintains an annotation protocol throughout the session. Deleted stray `.agents/skills/Untitled` file.

### 2026-03-02T1020 — Migrated session log to `vibed/` format

Moved `VIBED.md` (root) → `vibed/2026-03-02T0900.simplify-readme-and-config.md`, adopting the file naming convention defined in the new `vibe-code` skill.

### 2026-03-02 — Filename format: YYMMDDHHmm

Updated the `vibe-code` skill so session log filenames use `YYMMDDHHmm.<goal-slug>.md` (e.g. `2603020900.simplify-readme-and-config.md`). Renamed current session file from `2026-03-02T0900.simplify-readme-and-config.md` to `2603020900.simplify-readme-and-config.md`.

### 2026-03-02 — Docs skill created and refined

Created `.agents/skills/docs/SKILL.md` (auto-applied when work touches README, docs/, or linking from AGENTS.md to docs). Scope: human documentation only; AGENTS.md is agent instructions, managed separately. Content added: (1) Goal = move content from README into docs, no duplication — replace/remove in README when moving. (2) Root README = marketing/lighter; docs/README.md = exhaustive index. (3) One page = one folder (kebab-case) with README.md inside; per-folder README by default; non-README files and subfolders = kebab-case. (4) Cross-linking with relative URLs so docs are navigable on GitHub. (5) External links inline in the doc content. (6) AGENTS.md may link to docs or state that docs exist so the agent can read them. Skill is ready to support the README decomposition and docs structure work when we execute it.

### 2026-03-02 — Providers docs and feature matrix

Created `docs/providers/` with index (README.md) and one subfolder per provider (claude, copilot, codex, opencode, cursor, antigravity), each with README.md. Moved engine setup, install, and HAL usage from root README into these files; root README now links to docs/providers and keeps a short Engine Configuration section plus Providers (model list) and Model defaults. Added docs/README.md as main docs index. Researched each provider: (1) Instruction files and precedence — documented in each provider README (AGENTS.md vs CLAUDE.md/GEMINI.md, chain/merge behavior, what happens when both exist). (2) Per-user vs shared session — Claude and Antigravity use session ID (HAL stores per user); Copilot, Codex, OpenCode, Cursor use “continue last” only, so session shared by all users. Added a Feature compatibility table in docs/providers/README.md: rows = Instruction file, Main skills folder, Per-user session, Network access, Full disk access, YOLO mode, Streaming progress; columns = each provider. Added one-line session behavior (per-user vs shared) in each provider README under Sessions.

### 2026-03-02 — Config docs folder and structure

Created `docs/config/` and moved all configuration documentation out of the root README. Structure: (1) `docs/config/README.md` — config files (hal.config.json, local), env var substitution, globals table, projects[] table, project slug, dataDir, log files, directory structure; links to Context, Commands, Engine, and Providers. (2) `docs/config/context/README.md` — full context injection (implicit keys, custom context, variable patterns, hooks, prompt format). (3) `docs/config/commands/README.md` — built-in command config (enabled, messages, /start, /help, /reset, /clean, HAL_COMMANDS, skills visibility). (4) `docs/config/engine/README.md` — generic engine fields, providers model list, model defaults; per-engine details stay in docs/providers and are linked from here. Root README Configuration section replaced with a short bullet list linking to the five config docs and a collapsible minimal config example. Updated docs/README.md index to include Configuration. Fixed Migration section link to point to docs/config for env var substitution.

### 2026-03-02 — README engines table, Quick Start merge, docs split, license

- **Engines:** Copied feature comparison table into root README; reordered columns to OpenCode, Codex, Claude Code, Copilot, Cursor, Antigravity; added bullet list of engine links above table so users see all engines at once without scrolling. Terminology: "engine" = config choice, "provider" = product/docs.
- **Quick Start:** Kept only Quick Start; merged Installation and CLI Commands into it, with `--cwd ./workspace` examples for init and start.
- **Docs split:** Moved Custom Commands → `docs/custom-commands/README.md`, Skills → `docs/skills/README.md`, Telegram (Creating bot + User ID) → `docs/telegram/README.md`, Voice messages (Whisper) → `docs/voice/README.md`. Root README now has short "Custom commands and skills", "Telegram", "Voice messages" sections with links. Updated `docs/README.md` index.
- **Cleanup:** Removed "Migration from v1" section from root README.
- **License:** License set to MIT in README; added note at bottom: "This project is forked by the CCP at Telegram." (package.json already had MIT.)

### 2026-03-02 — Engine terminology, docs/engines rename, config subsections, Telegram deep links

- **Terminology:** Consolidated on "engine" in README and docs (config, code, and prose). Replaced "provider" with "engine" where it meant the runnable (claude, codex, etc.); kept "provider" only for OpenCode product wording. Updated docs/providers/README.md title and body, docs index, config engine doc, skills, custom-commands; "Back to engines index" in per-engine READMEs.
- **Folder rename:** Renamed `docs/providers` → `docs/engines`; updated all links in README and docs (config, config/engine, config/context, skills, docs index).
- **Config structure:** Merged "Engine and models" into `docs/engines/README.md` (engine selection, generic fields, model list `providers` key, model defaults). Removed `docs/config/engine/`. Created `docs/config/logging/README.md` (level, flow, persist, log file paths) and `docs/config/rate-limit/README.md` (max, windowMs, per-user-per-bot, window behaviour from code). Config README: single rows for logging and rateLimit with deep links; projects table gained `logging` and `rateLimit` overrides; See also includes Logging and Rate limit. Root README Configuration list: one Engines link, Logging, Rate limit.
- **Telegram:** Root README Telegram section: two bullets with deep links — "Register a bot" → `docs/telegram/README.md#creating-a-telegram-bot`, "Find your user ID" → `#finding-your-telegram-user-id`. Enriched `docs/telegram/README.md`: step-by-step BotFather (open chat, /newbot, copy token, add to .env.local + `${VAR}` in config, one bot per project) and Finding user ID (@userinfobot, add to allowedUserIds global/project, why use allowlist). Link to config env substitution.

### 2026-03-02 — Voice doc transcription config; allowedUserIds clarification

- **Voice messages:** Added a **Configuration** section to `docs/voice/README.md`: table for `transcription.model` and `transcription.showTranscription` (globals and per-project), two JSON examples, link to main config doc. Keeps voice setup and transcription options in one place.
- **allowedUserIds:** User asked where their Telegram user ID is in `examples/hal.config.json`. Clarified in reply: empty `allowedUserIds: []` means **open access** (no allowlist; anyone can use both bots), so no user ID is required. User ID only goes in config when restricting access — in `globals.access.allowedUserIds` (same for all projects) or per-project `access.allowedUserIds`. No doc or config change; reverted a brief attempt to add a JSON “comment” key.
