# Plan — 067: Rename config-dir flag to config

## Phase 1 — Core CLI changes (`src/cli.ts`) ✅

### Step 1.1 — Update `ParsedArgs` interface ✅
Add `configFile?: string` alongside the existing `configDir: string`.

### Step 1.2 — Rewrite the `--config` parsing block in `parseArgs()` ✅
Replace the two `--config-dir` / `--config-dir=` branches with `--config` / `--config=` branches.

Detection logic (applied after resolving the raw value to an absolute path):
- Value has **no extension** → directory mode: set `configDir`, leave `configFile` undefined.
- Value has an extension in `CONFIG_EXTENSIONS` (`.json`, `.jsonc`, `.yaml`, `.yml`) → file mode:
  - Crash with a clear message if the file does not exist.
  - Set `configDir` to `dirname(resolved)`, set `configFile` to `basename(resolved)`.
- Value has **any other extension** → crash immediately with: `Error: unsupported config extension "<ext>". Supported: .json, .jsonc, .yaml, .yml`.

Implemented as `parseConfigArg()` (exported for testing), called from the `--config` / `--config=` branches.

### Step 1.3 — Update `showHelp()` ✅
- Replaced `--config-dir <path>` with `--config <path>  Config directory, or explicit config file path (supported: .json, .jsonc, .yaml, .yml)`
- Replaced both `--config-dir` occurrences in the Examples section with `--config`.

### Step 1.4 — Thread `configFile` through the call chain ✅
- `parseArgs()` returns `configFile` in `ParsedArgs`.
- `main()` destructures `configFile` and passes it to `runStart()`.
- `runStart(configDir, wizardPrefill, configFile?)` forwards `configFile` to both `tryLoadMultiConfig` calls (initial load + hot-reload).
- `runInit` uses `configDir` as before (init always writes a new file; file-path mode doesn't apply).

---

## Phase 2 — Config loader (`src/config.ts`) ✅

### Step 2.1 — Update `tryLoadMultiConfig` signature ✅
```ts
export function tryLoadMultiConfig(
  configDir: string,
  configFile?: string,
): LoadedConfigResult
```
Forwards `configFile` to `loadMultiConfigInternal`.

### Step 2.2 — Update `loadMultiConfigInternal` ✅
When `configFile` is provided:
- Resolves the full path as `join(configDir, configFile)`.
- Determines its format from the extension (using `CONFIG_EXTENSIONS`).
- Loads that file directly, bypassing the `resolveConfigFile` scan.
- Produces the same `LoadedConfigResult` shape as the directory case.

When `configFile` is absent, behaviour is unchanged.

---

## Phase 3 — `package.json` scripts ✅

Replaced `--config-dir` with `--config` in all five affected scripts:
- `start`
- `dev`
- `start:test`
- `dev:test`
- `wiz:test`

---

## Phase 4 — Docs and README ✅

Updated every occurrence of `--config-dir` to `--config` in:
- `README.md`
- `docs/config/README.md`
- `docs/config/env-files/README.md`
- `docs/setup-wizard/README.md`
- `docs/development/README.md`

Prose uses of `{config-dir}` (path examples like `{config-dir}/.env`) left as-is — they describe the concept, not the CLI flag.

---

## Phase 5 — Tests ✅

### Step 5.1 — Locate or create a CLI unit-test file ✅
Created `src/cli.test.ts`.

### Step 5.2 — Test `parseArgs` for the `--config` argument ✅
Added 6 test cases covering:
1. `--config ./some/dir` → directory mode; `configDir` set, `configFile` undefined.
2. Equals-form `--config=./some/dir` → same as (1).
3. `--config ./some/dir/hal.config.json` (file exists) → `configDir = ./some/dir`, `configFile = hal.config.json`.
4. `--config ./configs/hal.config.yaml` (file exists) → file mode with `.yaml`.
5. `--config ./missing/hal.config.json` (file absent) → process exits with error.
6. `--config ./bad.xyz` → process exits with unsupported-extension error.

### Step 5.3 — Verify existing tests still pass ✅
All 20 tests pass (14 pre-existing + 6 new). TypeScript typecheck passes.
