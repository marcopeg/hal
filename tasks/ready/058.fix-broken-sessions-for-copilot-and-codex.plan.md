# Plan: 058 — Fix broken session continuity across adapters

## Overview

Restore session continuity for shared/cwd-scoped adapters (Copilot, Codex, Cursor, OpenCode) by treating `session.json` as the continuation marker, while introducing explicit adapter session capabilities for validating `engine.session` modes. Unsupported `"user"` isolation must fail during config load (including hot reload), and recover automatically after config correction.

## Phase 1 — Define adapter session capabilities

1. Add session capability metadata to the engine adapter contract in `src/engine/types.ts`.
2. Define capability fields needed by config/session policy (minimum: supports user-isolated sessions; optional: supports shared continuation marker).
3. Implement capabilities in all adapters:
   - `src/engine/adapters/claude.ts`
   - `src/engine/adapters/antigravity.ts`
   - `src/engine/adapters/codex.ts`
   - `src/engine/adapters/cursor.ts`
   - `src/engine/adapters/opencode.ts`
   - `src/engine/adapters/copilot.ts`
4. Ensure capability values match real CLI semantics (true user isolation only where resume-by-ID is supported).

## Phase 2 — Centralize session mode validation at config-load time

1. Replace hardcoded engine-name checks in `src/config.ts` session resolution with capability-driven validation.
2. Validate `engine.session: "user"` by querying adapter capabilities for the configured engine.
3. Keep error messaging explicit and actionable (engine name + unsupported mode + allowed alternatives).
4. Ensure validation remains in config load/resolve flow so failures occur at startup and watcher reload, not at first message.

## Phase 3 — Restore continuation marker flow across handlers

1. Refactor session load decision in all message handlers to support shared/cwd-scoped continuation marker usage:
   - `src/bot/handlers/text.ts`
   - `src/bot/handlers/photo.ts`
   - `src/bot/handlers/document.ts`
   - `src/bot/handlers/voice.ts`
2. For `engine.session !== false`, load `session.json` marker for adapters/modes that continue via `--continue`/`-c`/`resume --last`.
3. Preserve existing per-user resume-ID flow for true user-isolated adapters/modes.
4. Keep stateless behavior unchanged for `engine.session === false` (no read/write session file).
5. Remove duplicated conditional logic where possible to reduce drift between handlers.

## Phase 4 — Align adapter execute logic with marker semantics

1. Verify each shared/cwd-scoped adapter enables continuation when marker session is present:
   - Copilot: `--continue`
   - Codex: `exec resume --last` for non-user mode
   - Cursor: `--continue` for non-user mode
   - OpenCode: `-c`
2. Ensure adapters continue returning/storing placeholder session marker (`"active"`) in non-user modes.
3. Ensure user mode remains strict resume-by-ID where supported; no silent downgrade when `"user"` requested.
4. Keep `/clean` and explicit `continueSession: false` behavior intact (fresh run when requested).

## Phase 5 — Hot reload failure and recovery verification

1. Confirm config watcher reload path already retries on subsequent changes and does not require process restart after a failed reload.
2. Validate unsupported `engine.session: "user"` now fails during reload with clear logs/errors.
3. Validate recovery path: after fixing config to supported mode (`true` or `"shared"`), next reload succeeds and bots resume handling messages.
4. If error surfacing is unclear, tighten logging around reload failure reasons in existing startup/reload code path.

## Phase 6 — Regression coverage and documentation artifacts

1. Add/update manual regression checklist under `manual-tests/` covering:
   - second-message continuation for Copilot/Codex/Cursor/OpenCode
   - user isolation where supported
   - unsupported `"user"` mode load-time rejection
   - hot-reload fail-and-recover scenario
2. Add or adjust automated tests where practical for:
   - config validation rejecting unsupported `"user"` mode
   - handler session marker load behavior
3. Run quality gates:
   - `pnpm run lint`
   - `pnpm run build`
4. Update task notes/findings with any implementation caveats discovered during execution.

## File touch forecast

- `src/engine/types.ts`
- `src/engine/adapters/claude.ts`
- `src/engine/adapters/antigravity.ts`
- `src/engine/adapters/codex.ts`
- `src/engine/adapters/cursor.ts`
- `src/engine/adapters/opencode.ts`
- `src/engine/adapters/copilot.ts`
- `src/config.ts`
- `src/bot/handlers/text.ts`
- `src/bot/handlers/photo.ts`
- `src/bot/handlers/document.ts`
- `src/bot/handlers/voice.ts`
- `manual-tests/*` (new checklist file)

## Risks and sequencing notes

1. Type-level adapter contract changes will require all adapters to compile before moving on; complete Phase 1 first to avoid piecemeal breakage.
2. Config validation should remain deterministic and side-effect-free; avoid calling external CLIs during validation.
3. Shared-marker behavior can unintentionally affect first-message semantics; verify that continuation is only attempted after marker exists.
4. Keep backward compatibility for existing valid configs (`false`, `true`, `"shared"`, and supported `"user"`).
