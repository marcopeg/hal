# 076 — Allow /clean to pass first message for new session

**Status**: draft

## Problem

`/clean` resets the current session, but if the user wants to immediately start a new flow they must send a second message. This adds friction and makes "clean + start new prompt" a two-step process.

## Goal

Allow `/clean` to accept optional text and automatically send it as the **first message** of the new session after cleanup.

## Context & Constraints

- `/clean` should still work with no extra text (current behavior).
- The provided text must be treated exactly like a normal user message for a new session (same pipeline, logging, and engine routing).
- The message must **not** be attached to the previous session transcript.
- Scope for this task is **Telegram only**.
- When `/clean` includes a payload, that payload overrides the configured `commands.clean.message` reply for that invocation.
- Session reset semantics must remain engine-correct:
  - engines with active reset behavior must still perform the real reset step first;
  - payload dispatch must start a new session (no continuation).

## What to do

### Command parsing

- Support `/clean <message>` where everything after `/clean ` is the new first message.
- Treat everything after `/clean ` as a single message and send it to the LLM as normal user input.
- If the message is empty or only whitespace, behave like `/clean` with no payload.

### Post-clean dispatch

- After session cleanup completes, enqueue the message for the engine as if the user had just sent it.
- Ensure the UI/telegram response shows the exact normal message processing/status flow.
- For `/clean <message>`, do not send an extra configured clean confirmation; dispatch the payload immediately after successful reset.

### Safety & UX

- If cleanup fails, do not send the message; surface the error.
- Reuse existing message-handling validation/limits from the normal text pipeline.

## Acceptance criteria

- [ ] `/clean` without a message behaves exactly as today.
- [ ] `/clean <message>` resets the session and sends `<message>` as the first message in the new session.
- [ ] Everything after `/clean ` is treated as one message and passed to the LLM as normal user input.
- [ ] The dispatched message appears in the new session history only.
- [ ] For `/clean <message>`, the configured `commands.clean.message` reply is not sent; the payload is processed immediately.
- [ ] Task scope is Telegram command handling only; no CLI command-surface changes are required.
- [ ] Errors during cleanup prevent dispatch and return a clear error to the user.
- [ ] Engine session behavior remains correct: reset first, then payload runs as a fresh turn without continuation.
