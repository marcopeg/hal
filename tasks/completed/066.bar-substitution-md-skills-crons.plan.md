# Plan — 066 Prompt body substitution for cron `.md` files

## Overview

The change is small and surgical. One helper (`substituteMessage`) already exists and does
exactly what's needed — it just isn't called for cron prompt bodies. The plan has three
phases: wire the helper in, add tests, update docs.

---

## Phase 1 — Core implementation ✅

**File:** `src/crons/executor-md.ts`

### Step 1.1 — Add `substituteMessage` to the import

`substituteMessage` is already exported from `src/context/resolver.ts`. The file already imports
`buildCronContextVars`, `buildSystemContext`, and `formatContextPrompt` from that module.
Add `substituteMessage` to the same import statement.

### Step 1.2 — Apply substitution in `executeMdCron`

After line 93 (`contextVars["cron.lastRun"] = ...`), before the `formatContextPrompt` call:

```ts
const resolvedPrompt = substituteMessage(def.prompt, contextVars, logger);
const contextualPrompt = formatContextPrompt(contextVars, resolvedPrompt);
```

Replace the current `formatContextPrompt(contextVars, def.prompt)` call.

### Step 1.3 — Apply substitution in `executeMdProjectCron`

Same pattern after line 184 (`contextVars["cron.lastRun"] = ...`):

```ts
const resolvedPrompt = substituteMessage(def.prompt, contextVars, logger);
const contextualPrompt = formatContextPrompt(contextVars, resolvedPrompt);
```

Replace the current `formatContextPrompt(contextVars, def.prompt)` call.

---

## Phase 2 — Tests ✅

> Added vitest as devDependency (first test infrastructure in the project). Added `test` and `test:watch` scripts to package.json.

No test files exist under `src/crons/` yet. Add a focused unit test file.

**File:** `src/crons/executor-md.test.ts`

Test the substitution step in isolation by calling `substituteMessage` directly with a
mock `contextVars` map (mirrors what the executor does), without needing to spin up
a full cron executor:

| Test case | Input | Expected output |
|-----------|-------|-----------------|
| Context key ref | `"Hello ${bot.firstName}"` + `{ "bot.firstName": "Alice" }` | `"Hello Alice"` |
| Env var ref | `"Token: ${MY_TOKEN}"` + `{}` + `process.env.MY_TOKEN = "abc"` | `"Token: abc"` |
| Shell expansion | `"Date: @{echo 2026-01-01}"` + `{}` | `"Date: 2026-01-01"` |
| Unresolved key | `"Val: ${missing.key}"` + `{}` | `"Val: "` (empty string) |
| Cron key ref | `"Run #${cron.runs}"` + `{ "cron.runs": "3" }` | `"Run #3"` |

These tests require no mocking of executor internals — they test the substitution helper
in the same way the executor uses it.

---

## Phase 3 — Documentation ✅

### Step 3.1 — `docs/crons/project/README.md`

- **Remove** the existing note at line 158:
  > _Variable substitution applies to `.md` frontmatter only. The prompt body is sent as-is..._

- **Add** a new `### Prompt body substitution` subsection inside the `.md` section
  (after the context injection table), documenting that `${}` and `@{}` work in the
  prompt body:

  ```markdown
  ### Prompt body substitution

  `${}` and `@{}` patterns in the `.md` prompt body are resolved at execution time,
  after the context map is fully built and before the context header is prepended.

  | Pattern | Evaluated | Description |
  |---------|-----------|-------------|
  | `${expr}` | Per execution | Looks up `expr` in the resolved context map, then `process.env` |
  | `@{cmd}` | Per execution | Runs a shell command fresh each time |

  Unresolved keys (absent from both the context map and `process.env`) expand to an
  empty string.

  **Example:**
  ```markdown
  ---
  enabled: true
  schedule: "0 8 * * 1-5"
  runAs: ${MY_USER_ID}
  ---

  Good morning ${bot.firstName}! Today is ${sys.date}.
  Summarise commits since yesterday.
  ```

  Note: `#{cmd}` (boot-time shell) is not supported in prompt bodies. Use `@{cmd}` for
  execution-time shell expansion.

- Update the `Context injection` subsection note to reflect that vars are available in
  the prompt body too.

### Step 3.2 — `docs/crons/system/README.md`

Apply the same changes as Step 3.1 in the system-tier crons doc:
- Remove the "prompt body is sent as-is" note.
- Add the same `### Prompt body substitution` subsection.

### Step 3.3 — `docs/skills/README.md`

No change needed. The existing note is correct:
> _The SKILL.md prompt body is sent as-is. There is no variable substitution applied._

---

## Execution order (for reference)

```
executeMdCron / executeMdProjectCron:
  1. buildCronContextVars(...)          → contextVars (sys.*, bot.*, project.*, engine.*)
  2. contextVars["cron.runs"] = ...     → add cron state
  3. contextVars["cron.lastRun"] = ...
  4. substituteMessage(def.prompt, contextVars, logger)   ← NEW
  5. formatContextPrompt(contextVars, resolvedPrompt)
  6. agent.call(contextualPrompt)
```
