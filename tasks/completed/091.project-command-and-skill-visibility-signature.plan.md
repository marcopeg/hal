# Plan - 091 Project command and skill visibility signature

## Objective

Implement a unified Telegram visibility model for project/global `.mjs` commands and skills, including strict boot-time validation, same-name command/skill precedence, and menu/help fallback when a higher-precedence command is disabled.

## [x] Phase 1 - Normalize command and skill metadata

1. Extend command loading in [`/Users/marcopeg/dv/hal/src/bot/commands/loader.ts`](/Users/marcopeg/dv/hal/src/bot/commands/loader.ts) so project/global `.mjs` commands can export `enabled`, `showInMenu`, and `showInHelp`, defaulting each to `true` when omitted.
2. Replace the current skill `telegram: true` boolean-only parsing with a structured parser that accepts:
   - `telegram: true`
   - `telegram: false`
   - `telegram.enabled`
   - `telegram.showInMenu`
   - `telegram.showInHelp`
3. Expand the in-memory command/skill metadata model so routing and visibility decisions use normalized booleans rather than the current single `telegram?: boolean` flag.
4. Define one explicit normalization path for same-name collisions so a disabled `.mjs` command does not suppress a lower-precedence Telegram-enabled skill in menu/help or routing.

Execution notes:
- Implemented normalized `enabled`, `showInMenu`, and `showInHelp` fields on command entries in [`/Users/marcopeg/dv/hal/src/bot/commands/loader.ts`](/Users/marcopeg/dv/hal/src/bot/commands/loader.ts).
- Replaced regex-only skill Telegram parsing with YAML frontmatter parsing so object-form visibility is supported without adding a second parsing path.

## [x] Phase 2 - Add validation and startup failure behavior

1. Introduce validation for `.mjs` command metadata exports:
   - `enabled`
   - `showInMenu`
   - `showInHelp`
2. Introduce validation for skill `telegram` frontmatter:
   - boolean shorthand is allowed
   - object form is allowed only with `enabled`, `showInMenu`, and `showInHelp`
   - all allowed fields must be booleans
   - unknown keys are invalid
3. Make validation errors fatal at boot/reload time rather than warning and skipping invalid entries.
4. Ensure error messages identify the offending file and field so bad metadata is actionable during startup.

Execution notes:
- Added fatal validation for invalid `.mjs` visibility exports and invalid skill `telegram` shapes/types.
- Kept non-visibility problems such as missing `description` on the existing warn-and-skip path to avoid broadening failure behavior beyond the task scope.

## [x] Phase 3 - Update resolution and visibility behavior

1. Update command list assembly in [`/Users/marcopeg/dv/hal/src/bot/commands/loader.ts`](/Users/marcopeg/dv/hal/src/bot/commands/loader.ts) so disabled project/global `.mjs` commands are not treated as active command surfaces.
2. Preserve current precedence for enabled surfaces:
   - project command
   - system/global command
   - skill
   - npm and built-ins per existing behavior
3. Update `commandsForTelegramMenu` and `commandsForHelp` so menu/help publication uses normalized per-entry visibility flags and same-name fallback semantics.
4. Update slash-command interception in [`/Users/marcopeg/dv/hal/src/bot/handlers/text.ts`](/Users/marcopeg/dv/hal/src/bot/handlers/text.ts) so disabled same-name `.mjs` commands fall through to skill resolution and then engine fallback.
5. Review `resolveCommandPath` / `resolveSkillEntry` responsibilities and adjust as needed so runtime routing can distinguish:
   - file exists but is disabled
   - file exists and is enabled
   - file does not exist

Execution notes:
- Disabled project/global `.mjs` commands are now skipped as active command surfaces, which lets lower-precedence enabled skills become the visible menu/help entry.
- `resolveCommandPath` became visibility-aware and async so slash-command and callback routing can respect `enabled: false`.

## [x] Phase 4 - Wire startup, hot reload, and publishing flows

1. Update startup menu publication in [`/Users/marcopeg/dv/hal/src/bot.ts`](/Users/marcopeg/dv/hal/src/bot.ts) to use the new normalized visibility data.
2. Update hot-reload menu publication in [`/Users/marcopeg/dv/hal/src/bot/commands/watcher.ts`](/Users/marcopeg/dv/hal/src/bot/commands/watcher.ts) to preserve the same behavior and failure mode as boot.
3. Update HAL help rendering in [`/Users/marcopeg/dv/hal/src/bot/commands/message.ts`](/Users/marcopeg/dv/hal/src/bot/commands/message.ts) so disabled same-name commands fall through to skill descriptions when the skill is Telegram-exposed.
4. Confirm that fatal validation on reload degrades in the same way as current config/boot failures, without silently publishing stale invalid command metadata.

Execution notes:
- Startup and hot-reload publication both reuse the normalized loader path, so the same validation and precedence rules apply in both places.
- Help rendering in [`/Users/marcopeg/dv/hal/src/bot/commands/message.ts`](/Users/marcopeg/dv/hal/src/bot/commands/message.ts) now uses the filtered command list directly instead of separately re-checking the old skill flag.

## [x] Phase 5 - Test the behavior matrix

1. Extend [`/Users/marcopeg/dv/hal/src/bot/commands/loader.test.ts`](/Users/marcopeg/dv/hal/src/bot/commands/loader.test.ts) to cover:
   - command defaults
   - skill shorthand and object forms
   - `telegram: false`
   - per-surface visibility independence
   - same-name command/skill fallback in menu/help
2. Extend [`/Users/marcopeg/dv/hal/src/bot/handlers/text.test.ts`](/Users/marcopeg/dv/hal/src/bot/handlers/text.test.ts) to cover routing when:
   - same-name command is enabled
   - same-name command is disabled and skill is enabled
   - command and skill are both disabled
3. Add validation tests for invalid `.mjs` exports and invalid skill frontmatter shapes/types.
4. Run the focused test suite plus project validation:
   - `npm run test -- src/bot/commands/loader.test.ts src/bot/handlers/text.test.ts`
   - `npm run lint`
   - `npm run build`

Execution notes:
- Expanded loader tests to cover same-name fallback, object-form skill visibility, `telegram: false`, and fatal validation.
- Expanded text handler tests to cover skill fallback when no enabled custom command matches and command-resolution failure handling.
- Validation run completed successfully for the focused tests, lint, and full build.

## [x] Phase 6 - Update docs and examples

1. Update skill authoring docs in [`/Users/marcopeg/dv/hal/docs/commands/skills/README.md`](/Users/marcopeg/dv/hal/docs/commands/skills/README.md) with the new `telegram` forms, defaults, and invalid examples.
2. Update command docs in [`/Users/marcopeg/dv/hal/docs/commands/project/README.md`](/Users/marcopeg/dv/hal/docs/commands/project/README.md) and [`/Users/marcopeg/dv/hal/docs/config/commands/README.md`](/Users/marcopeg/dv/hal/docs/config/commands/README.md) to document:
   - `.mjs` visibility exports
   - same-name command+skill pairing
   - disabled-command fallback to skills in routing/menu/help
3. Refresh examples under `examples/` where Telegram-exposed skills or commands are shown so the new signature appears in at least one command example and one skill example.
4. Verify the docs consistently describe fatal boot-time validation rather than warning-and-skip behavior.

Execution notes:
- Updated the command and skill docs to describe the shipped visibility model rather than the previous "not yet uniform" state.
- Refreshed the Codex `todo` command and skill examples to show the new `.mjs` visibility exports and skill object-form `telegram` signature.

## Execution summary

- All planned phases were completed.
- The implementation stayed aligned with the approved plan; the main tactical choice was to parse skill frontmatter as YAML instead of extending the previous regex parser, because the object-form `telegram` contract requires structured validation.

## Success criteria

- The runtime uses one consistent visibility model for project/global `.mjs` commands and skills.
- A disabled same-name command no longer shadows an enabled skill in routing, Telegram menu publication, or HAL help.
- Invalid command/skill Telegram visibility metadata fails startup with actionable errors.
- Existing command files and `telegram: true` skills continue to work unchanged.
