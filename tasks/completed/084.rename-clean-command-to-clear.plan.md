# 084 — Rename /clean command to /clear — Plan

Execution status: In progress (implementation complete, awaiting manual Telegram spot-checks / review handoff)

## Execution Notes

- Decision update: legacy `commands.clean` is not supported; startup now fails fast with a clear config error.
- Scope guard: no `/new` or response-template standardization work from task `082` was added.

## Phase 1: Rename command surface in runtime code

### 1.1 — Rename command handler API to `clear`
- [x] Renamed `DEFAULT_CLEAN_TEMPLATE` -> `DEFAULT_CLEAR_TEMPLATE` and `createCleanHandler(...)` -> `createClearHandler(...)` in `src/bot/commands/session.ts`.
- [x] Updated handler comments/docstrings to `/clear`.
- [x] Kept reset behavior intact (naming-only change).

### 1.2 — Register `/clear` and remove `/clean` routing
- [x] Switched command registration to `bot.command("clear", ...)` in `src/bot.ts`.
- [x] Updated import/usage to `createClearHandler`.
- [x] Confirmed no `/clean` route remains in bot command wiring.

### 1.3 — Keep command-list internals in sync
- [x] Renamed built-in command metadata from `clean` to `clear` in `src/bot/commands/loader.ts`.
- [x] Renamed `CommandEnabledFlags.clean` to `CommandEnabledFlags.clear` and updated usages.
- [x] Verified menu generation path uses the `clear` flag and command name.

---

## Phase 2: Enforce config key `commands.clear` only

### 2.1 — Add canonical `clear` key in config schema/types
- [x] Added canonical `clear` command key in `CommandsConfigSchema` and `ResolvedProjectConfig["commands"]`.
- [x] Updated resolved command object construction to expose `commands.clear`.
- [x] Updated message-template label to `commands.clear`.

### 2.2 — Reject legacy key
- [x] Removed legacy fallback resolution from `commands.clean`.
- [x] Resolved runtime config uses only `commands.clear`.

### 2.3 — Emit clear configuration error when legacy key is used
- [x] Added startup errors in `src/cli.ts` for both `globals.commands.clean` and `projects.<key>.commands.clean`.
- [x] Error text points users to `commands.clear`.

---

## Phase 3: Update command-message and helper references

### 3.1 — Update shared message builder flags
- [x] Replaced `clean` enabled flag usage with `clear` in `src/bot/commands/message.ts`.
- [x] Updated command-message inline docs from `/clean` to `/clear`.

### 3.2 — Update engine/session comments to avoid stale naming
- [x] Updated stale reset-command naming comments in `src/engine/types.ts`, `src/user/setup.ts`, and `src/bot/commands/session.ts`.
- [x] Functional behavior unchanged.

---

## Phase 4: Documentation and migration communication

### 4.1 — Update docs command naming and config examples
- [x] Updated command naming to `/clear` in:
  - [x] `README.md`
  - [x] `docs/config/README.md`
  - [x] `docs/config/commands/README.md`
  - [x] `docs/custom-commands/README.md`
  - [x] `docs/engines/README.md`
  - [x] `docs/engines/claude/README.md`
  - [x] `docs/engines/antigravity/README.md`
  - [x] `docs/engines/copilot/README.md`
  - [x] `docs/engines/cursor/README.md`
  - [x] `docs/engines/codex/README.md`
  - [x] `docs/engines/opencode/README.md`
  - [x] `AGENTS.md`
  - [x] `examples/blog/src/content/posts/drafts/hal-commands.mdx`
- [x] Updated config examples/references to `commands.clear` in docs (`docs/config/commands/README.md`, `docs/config/reference.yaml`).

### 4.2 — Keep overlap with task 082 out of scope
- [x] Kept task `082` work out of scope.
- [x] Limited changes to rename/migration behavior and documentation updates.

### 4.3 — Add migration note
- [x] Added migration note to `CHANGELOG.md`.
- [x] Added migration guidance note in `docs/config/commands/README.md`.

---

## Phase 5: Verification and regression checks

### 5.1 — Functional checks
- [x] Verified command wiring and loader metadata now target `/clear` only.
- [x] Verified no `/clean` command route remains.
- [x] Verified `commands.clear` is used in runtime command settings.
- [x] Verified legacy `commands.clean` is rejected with startup configuration errors.

### 5.2 — Static quality gates
- [x] `npm run lint` (pass)
- [x] `npm run test` (pass)
- [x] `npm run build` (pass)

### 5.3 — Manual spot checks (Telegram)
- [ ] With default config, verify `/clear` appears in command menu and `/clean` does not.
- [ ] With `commands.clear.enabled: false`, verify `/clear` is absent from menu/handler.
- [ ] With legacy `commands.clean` only, verify startup fails with a clear configuration error.
- [ ] Verify command-list placeholders (`${HAL_COMMANDS}`) display `/clear` and not `/clean`.

Execution note: manual Telegram checks were not executed in this session; static checks and source-level verification are complete.
