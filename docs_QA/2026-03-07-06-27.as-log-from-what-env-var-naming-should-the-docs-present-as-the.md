## Question

- **Where found**: `docs/setup-wizard/README.md` → “Config directory and format” (Secrets bullet)
- **What’s unclear / risky**: The docs say the wizard writes placeholders like `${TELEGRAM_BOT_TOKEN}`, but the wizard actually generates per-project placeholders like `${<PROJECT_KEY>_TELEGRAM_TOKEN}` (and user IDs like `${TELEGRAM_USER_ID}`, `${TELEGRAM_USER_ID_2}`, …). This can confuse users copying env var names.
- **Question**: What env var naming should the docs present as the canonical wizard output for bot tokens?

## Answer

a

## Source

- `docs/setup-wizard/README.md` → “Config directory and format”
