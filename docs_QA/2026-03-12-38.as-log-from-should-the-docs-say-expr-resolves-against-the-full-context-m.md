## Question
Should the docs say `${expr}` resolves against the full context map (implicit + configured keys) before falling back to env vars?

## Answer
I agree

## Source
docs/config/context/README.md — Variable substitution patterns
