## Question
For the context key `project.slug`, should it represent the **project key** (config slug) or the **path-derived slug** (current behavior)?

## Answer
The slug is legacy and should be removed or fixed. We moved to “projects as a map” so now each project has a key, not a slug. Mind this could need to be updated in multiple places

## Source
docs/config/context/README.md — Implicit context
