globals:
  access:
    allowedUserIds:
      - ${USER_MARCO_IPHONE_ID}
providers:
  codex: null
  opencode: null
projects:
  obsidian:
    cwd: ./obsidian
    telegram:
      botToken: ${OBSIDIAN_TELEGRAM_TOKEN}
    engine:
      name: codex
  frontend:
    cwd: ./frontend
    engine:
      name: codex
    telegram:
      botToken: ${TIMETRACKER_TELEGRAM_TOKEN}
