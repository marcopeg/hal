# Optional: path to a custom env file (and its .local sibling); when set, config-dir .env is not loaded.
# env: ~/.hal/secrets.env

globals:
  access:
    allowedUserIds:
      - ${USER_MARCO_IPHONE_ID}
providers:
  codex: null
  opencode: null
projects:
  obsidian:
    cwd: ./obsidian
    telegram:
      botToken: ${OBSIDIAN_TELEGRAM_TOKEN}
    # engine:
    #   name: codex
    #   session: user
  timetracker:
    active: false
    cwd: ./timetracker
    engine:
      name: codex
    telegram:
      botToken: ${TIMETRACKER_TELEGRAM_TOKEN}
